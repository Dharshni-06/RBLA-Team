{
  "name": "rbla-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "clean:ports": "powershell -Command \"try { Get-NetTCPConnection -LocalPort 5000 -ErrorAction Stop | ForEach-Object { Stop-Process -Id $_.OwningProcess -Force -ErrorAction SilentlyContinue } } catch { exit 0 }\"",
    "predev": "npm run clean:ports",
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"cyan,magenta\" \"npm run server\" \"npm run client\"",
    "start": "concurrently -n \"BACKEND,FRONTEND\" -c \"cyan,magenta\" \"npm run server\" \"npm run client\"",
    "server": "npm run dev --prefix backend",
    "server:start": "npm start --prefix backend",
    "client": "npm start --prefix frontend",
    "client:build": "npm run build --prefix frontend",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend"
  },
  "dependencies": {
    "react-icons": "^5.5.0",
    "react-toastify": "^11.0.3"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "gh-pages": "^6.3.0"
  }
}
